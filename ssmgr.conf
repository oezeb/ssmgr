# Chain name for iptables traffic monitoring
CHAIN_NAME="SS_TRAFFIC"

# Path to ss-server binary
SS_BIN=$(which ss-server)

# Default per-user data limit in MB
LIMIT_MB=200

# File paths
USER_FILE="$PWD/ss-users.txt"
CONFIG_DIR="$PWD/configs"
PID_DIR="$PWD/pids"
LOG_DIR="$PWD/logs"
LOG_FILE="$PWD/ssmgr.log"
